package com.sirc.core.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta SIRC (identidad propia, orientada a decisión rápida).
object SircColors {
    val Profit = Color(0xFF1DB954)
    val Marginal = Color(0xFFF5A623)
    val NotProfit = Color(0xFFE5484D)

    val OverlayBackground = Color(0xE611161E)
    val OverlayBorder = Color(0x332A8CFF)

    val Accent = Color(0xFF2A8CFF)
    val OnDark = Color(0xFFEDF2F7)
    val OnDarkMuted = Color(0xFF9AA7B5)

    val Surface = Color(0xFF171C24)
    val SurfaceHigh = Color(0xFF1E2530)
    val Background = Color(0xFF0F131A)
}
